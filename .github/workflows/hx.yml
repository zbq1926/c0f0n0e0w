name: 每2小时混淆一次

on:
  # 1. 手动触发
  workflow_dispatch:
  # 2. 所有分支推送触发（规避无限循环 + 仅监控源文件修改）
  push:
    branches:
      - '**'  # 匹配所有分支（仅分支推送，排除标签）
    paths:
      - 'source.js'  # 仅监控源JS文件（替换为你的实际源文件路径）
      - '!obfuscated.js'  # 排除混淆后的目标文件（避免无限循环）
  # 3. 定时触发：每2小时运行一次（UTC时间，对应北京时间每2小时整点）
  schedule:
    - cron: '0 */2 * * *'

jobs:
  build-and-obfuscate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        # 关键：获取完整仓库历史，否则git push可能失败
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Obfuscator
        run: npm install javascript-obfuscator

      - name: Obfuscate from local source file
        run: |
          cat > obfuscate.js << 'EOF'
          const JavaScriptObfuscator = require('javascript-obfuscator');
          const fs = require('fs');
          const path = require('path');
          
          // 配置：替换为你的实际源文件和目标文件名称
          const sourceFileName = 'source.js'; // 源JS文件（需存在于仓库根目录）
          const outputFileName = 'obfuscated.js'; // 混淆后的目标文件
          const sourceFilePath = path.join(process.cwd(), sourceFileName);

          // 检查源文件是否存在
          if (!fs.existsSync(sourceFilePath)) {
            console.error('错误：在路径 \'' + sourceFilePath + '\' 未找到源文件。请确保仓库根目录有名为 \'' + sourceFileName + '\' 的文件。');
            process.exit(1);
          }

          // 读取源文件内容
          const originalCode = fs.readFileSync(sourceFilePath, 'utf8');

          // 检查源文件是否为空
          if (!originalCode || originalCode.trim().length === 0) {
            console.error('错误：源文件 ' + sourceFileName + ' 为空。');
            process.exit(1);
          }

          // 混淆配置（保持你的原有配置不变）
          const obfuscationOptions = {
              compact: true,
              controlFlowFlattening: false,
              controlFlowFlatteningThreshold: 0,
              deadCodeInjection: false,
              stringArray: true,
              stringArrayEncoding: ['base64'],
              stringArrayThreshold: 1.0,
              stringArrayRotate: true,
              stringArrayShuffle: true,
              stringArrayWrappersCount: 2,
              stringArrayWrappersChainedCalls: false,
              stringArrayWrappersParametersMaxCount: 3,
              renameGlobals: true,
              identifierNamesGenerator: 'mangled-shuffled',
              identifierNamesCache: null,
              identifiersPrefix: '',
              renameProperties: false,
              renamePropertiesMode: 'safe',
              ignoreImports: false,
              target: 'browser',
              numbersToExpressions: false,
              simplify: false,
              splitStrings: true,
              splitStringsChunkLength: 1,
              transformObjectKeys: false,
              unicodeEscapeSequence: true,
              selfDefending: false,
              debugProtection: false,
              debugProtectionInterval: 0,
              disableConsoleOutput: true,
              domainLock: []
          };

          // 执行混淆并写入目标文件
          const obfuscatedCode = JavaScriptObfuscator.obfuscate(originalCode, obfuscationOptions).getObfuscatedCode();
          fs.writeFileSync(path.join(process.cwd(), outputFileName), obfuscatedCode, 'utf8');
          console.log('成功将 \'' + sourceFileName + '\' 混淆并保存至 \'' + outputFileName + '\'。');
          EOF
          node obfuscate.js

      - name: Commit and push the obfuscated file
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # 添加混淆后的目标文件
          git add 'obfuscated.js'
          # 检查是否有变更，无变更则跳过提交推送
          if git diff --staged --quiet; then
            echo "No changes to commit, the obfuscated file is already up-to-date."
          else
            git commit -m "自动混淆：更新obfuscated.js"
            git push
          fi
